{
  "version": 2,
  "framework": null,
  "installCommand": "npm ci --omit=optional --ignore-scripts",
  "buildCommand": "npx nx repair || true && npx nx reset || true && NX_DAEMON=false NX_SKIP_NX_CACHE=1 npx nx build react-ui --verbose || npx vite build --config packages/react-ui/vite.config.ts --outDir dist/packages/react-ui",
  "outputDirectory": "dist/packages/react-ui",
  "env": {
    "NX_DAEMON": "false",
    "NX_SKIP_NX_CACHE": "1"
  },
  "functions": {
    "api/**": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "^/api/.*", "dest": "/api" },
    { "handle": "filesystem" },
    { "src": ".*", "dest": "/" }
  ]
}
