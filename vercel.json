{
  "version": 2,
  "framework": null,
  "installCommand": "npm ci --omit=optional --ignore-scripts",
  "buildCommand": "NX_DAEMON=false npx nx reset && NX_DAEMON=false npm run vercel:build",
  "outputDirectory": "dist/packages/react-ui",
  "functions": {
    "api/**": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "^/api/.*", "dest": "/api" },
    { "handle": "filesystem" },
    { "src": ".*", "dest": "/" }
  ]
}
